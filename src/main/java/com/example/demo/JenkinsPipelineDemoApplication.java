package com.example.demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main application class for Jenkins Pipeline Demo
 * 
 * This is a simple Spring Boot application that demonstrates
 * a complete CI/CD pipeline with Jenkins
 */
@SpringBootApplication
public class JenkinsPipelineDemoApplication {

    public static void main(String[] args) {
        SpringApplication.run(JenkinsPipelineDemoApplication.class, args);
    }
}